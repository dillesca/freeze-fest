# Local development settings
DATABASE_URL=postgresql://freeze_fest:freeze_fest@postgres:5432/freeze_fest
POSTGRES_USER=freeze_fest
POSTGRES_PASSWORD=freeze_fest
POSTGRES_DB=freeze_fest

# Admin controls (set your own secrets in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=dev-secret
ADMIN_EMAIL=

# Optional: outbound SMTP settings for admin notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false
# SMTP_SENDER=freeze-fest@example.com

# Optional: upload photos to Google Cloud Storage
# GCS_PHOTO_BUCKET=freeze-fest-photos
# GCS_PHOTO_BASE_URL=https://storage.googleapis.com/freeze-fest-photos
# GCS_PHOTO_CACHE_CONTROL=public, max-age=86400

# Optional: local photo import for development
# LOCAL_PHOTO_IMPORT_DIR=/Users/you/Downloads/FreezeFest2024
# LOCAL_PHOTO_IMPORT_EVENT_SLUG=2024

# Optional: chromecast appliction ID
# TODO Figure out if this is possible for development purposes
# CAST_APP_ID=sfsdfsfds
